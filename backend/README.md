# cyber-backend

## Environment

Set these variables in `backend/.env` before starting the API:

- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`
- `MODEL_API_URL_SINGLE`
- `MODEL_API_URL_BATCH`
- `PORT`

Use the Supabase service role key only on the backend. Do not expose it in the frontend.
